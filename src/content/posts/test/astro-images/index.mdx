---
title: "Astro Images Test"
summary: "Summary here"
tags: [ "Tag1", "Tag2" ]
---
import { Picture } from 'astro:assets';
import LandscapeImage from './large-landscape.jpg';


# Test Markdown with Images

This post is used to test `Image` and `Picture` renders.

```js
<Picture 
    src={LandscapeImage} 
    alt="Landscape" 
    formats={['avif', 'webp', 'jpeg']}
    /* you can only 'densities' or 'widths' */
    /* either */
    densities={[1.5, 2]} 
    /* or */
    widths={[240,540,720]}

    fallbackFormat={format}
/>
```


<Picture
    pictureAttributes={{ class: 'image lazy' }}
    src={LandscapeImage}
    alt="Landscape Image"
    densities={[1.5, 2]}
    formats={['avif', 'webp', 'jpeg']}
    loading="eager"
    fetchpriority="high"
/>
